create database misc2;

CREATE TABLE autos2 (
    auto_id INT UNSIGNED NOT NULL AUTO_INCREMENT,
    make VARCHAR(128),
    year INT,
    mileage INT,
    PRIMARY KEY (auto_id)
);

-- Create the user with password
CREATE USER 'fred2'@'localhost' IDENTIFIED BY 'zap2';
CREATE USER 'fred2'@'127.0.0.1' IDENTIFIED BY 'zap2';

-- Grant privileges
GRANT ALL PRIVILEGES ON misc2.* TO 'fred2'@'localhost';
GRANT ALL PRIVILEGES ON misc2.* TO 'fred2'@'127.0.0.1';
